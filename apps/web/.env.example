# Server-only
DATABASE_URL=postgresql://copilot:copilot@localhost:5432/treasury
SOLANA_RPC_URL=https://api.devnet.solana.com
LOG_LEVEL=info

# AI provider — pick one of: anthropic | openai | qvac
MODEL_PROVIDER=anthropic

# Anthropic (only required if MODEL_PROVIDER=anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# OpenAI (only required if MODEL_PROVIDER=openai)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# QVAC (only required if MODEL_PROVIDER=qvac; start the server with `qvac serve openai`)
QVAC_BASE_URL=http://localhost:11434/v1
QVAC_MODEL=QWEN3_600M_INST_Q4

# Public (shipped to browser)
NEXT_PUBLIC_APP_URL=http://localhost:3000
