DATABASE_URL=postgresql://copilot:copilot@localhost:5432/treasury
SOLANA_RPC_URL=https://api.devnet.solana.com
LOG_LEVEL=info

# Telegram approval bot
# 1. Create a bot via @BotFather and paste the token below.
# 2. Add the bot to a chat (group or 1:1) and find the chat id with @RawDataBot.
# 3. List the Telegram user ids allowed to approve/deny (find your own id with @userinfobot).
TELEGRAM_BOT_TOKEN=
TELEGRAM_APPROVAL_CHAT_ID=
APPROVER_TELEGRAM_IDS=

# How often the worker scans for new pending actions (ms). Default 5000.
# ACTION_POLL_INTERVAL_MS=5000
